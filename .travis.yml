language: minimal 

services: docker

before_install:
- docker pull tonyodell/rakudo-nightly:latest

script:
- docker run -v $(pwd):/tmp/test:rw tonyodell/rakudo-nightly:latest zef install --deps-only /tmp/test
- docker run -v $(pwd):/tmp/test:rw tonyodell/rakudo-nightly:latest zef test /tmp/test
